# hono-bun-boilerplace
A boilerplate for creating hono api with bun with Prisma, Postgres and Docker integrations

To install dependencies:
```sh
bun install
```

To run:
```sh
bun run dev
```

open http://localhost:3000
